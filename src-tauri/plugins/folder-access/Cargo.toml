[package]
name = "tauri-plugin-folder-access"
version = "0.1.0"
edition = "2021"
links = "tauri-plugin-folder-access"
build = "build.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
tauri = { version = "2", default-features = false }

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }
